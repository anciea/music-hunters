---
phase: 02-flutter-shell-search
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mobile/lib/features/search/search_screen.dart
  - mobile/lib/features/search/search_notifier.dart
  - mobile/lib/features/search/widgets/track_list_tile.dart
  - mobile/lib/features/search/widgets/source_badge.dart
  - mobile/lib/features/search/widgets/shimmer_list.dart
  - mobile/lib/features/search/widgets/empty_state.dart
  - mobile/lib/features/search/widgets/error_state.dart
autonomous: true
requirements: [SRCH-01, SRCH-02]

must_haves:
  truths:
    - "User can type a keyword and submit to trigger a search"
    - "Search results appear as a scrollable list of track cards"
    - "Each result card shows song title, artist, album art, duration, and source badge"
    - "Shimmer skeleton cards display while search is loading"
    - "First-launch empty state shows search suggestion text"
    - "Error state shows retry button that re-triggers search"
  artifacts:
    - path: "mobile/lib/features/search/search_notifier.dart"
      provides: "AsyncNotifier managing search state"
      contains: "AsyncNotifier"
    - path: "mobile/lib/features/search/widgets/track_list_tile.dart"
      provides: "Result card with album art, title, artist, duration, source badge"
      contains: "CachedNetworkImage"
    - path: "mobile/lib/features/search/widgets/source_badge.dart"
      provides: "Per-source colored chip badge"
      contains: "sourceBadgeColors"
    - path: "mobile/lib/features/search/widgets/shimmer_list.dart"
      provides: "Shimmer skeleton loading state"
      contains: "Shimmer.fromColors"
  key_links:
    - from: "mobile/lib/features/search/search_notifier.dart"
      to: "mobile/lib/core/api/music_api.dart"
      via: "ref.read(musicApiProvider).searchTracks(query)"
      pattern: "musicApiProvider"
    - from: "mobile/lib/features/search/search_screen.dart"
      to: "mobile/lib/features/search/search_notifier.dart"
      via: "ref.watch(searchNotifierProvider)"
      pattern: "searchNotifierProvider"
    - from: "mobile/lib/features/search/widgets/track_list_tile.dart"
      to: "mobile/lib/core/models/track_dto.dart"
      via: "TrackDto parameter"
      pattern: "TrackDto"
---

<objective>
Build the complete search screen: search bar with submit-on-enter, AsyncNotifier for search state, result list with track cards (album art, title, artist, duration, source badge), shimmer loading skeleton, empty states, and error state with retry.

Purpose: This is the core user-facing feature of Phase 2 — searching for music across all sources and seeing results.
Output: Fully functional search UI connected to the Phase 1 backend API.
</objective>

<execution_context>
@/Users/liou/py_project/musicdl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liou/py_project/musicdl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-flutter-shell-search/02-CONTEXT.md
@.planning/phases/02-flutter-shell-search/02-RESEARCH.md
@.planning/phases/02-flutter-shell-search/02-UI-SPEC.md
@.planning/phases/02-flutter-shell-search/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: core infrastructure the search screen consumes -->

From mobile/lib/core/api/music_api.dart:
```dart
@RestApi()
abstract class MusicApi {
  factory MusicApi(Dio dio, {String baseUrl}) = _MusicApi;
  @GET('/search')
  Future<SearchResponse> searchTracks(@Query('q') String query);
}
// Provider: musicApiProvider
```

From mobile/lib/core/models/track_dto.dart:
```dart
@freezed
class TrackDto with _$TrackDto {
  const factory TrackDto({
    required String trackId,
    String? songName, String? singers, String? album,
    String? coverUrl, int? durationS, String? duration,
    String? source, String? ext, int? bitrate,
    String? codec, String? fileSize, int? fileSizeBytes,
  }) = _TrackDto;
  factory TrackDto.fromJson(Map<String, dynamic> json) => _$TrackDtoFromJson(json);
}
```

From mobile/lib/core/models/search_response.dart:
```dart
@freezed
class SearchResponse with _$SearchResponse {
  const factory SearchResponse({
    required List<TrackDto> tracks,
    required List<String> warnings,
  }) = _SearchResponse;
  factory SearchResponse.fromJson(Map<String, dynamic> json) => _$SearchResponseFromJson(json);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search notifier and source badge widget</name>
  <read_first>
    - .planning/phases/02-flutter-shell-search/02-RESEARCH.md (Pattern 1: AsyncNotifier, source badge colors)
    - .planning/phases/02-flutter-shell-search/02-UI-SPEC.md (source badge spec, colors)
    - mobile/lib/core/api/music_api.dart (musicApiProvider)
    - mobile/lib/core/models/search_response.dart (SearchResponse type)
  </read_first>
  <files>
    mobile/lib/features/search/search_notifier.dart,
    mobile/lib/features/search/widgets/source_badge.dart
  </files>
  <action>
    1. Create `mobile/lib/features/search/search_notifier.dart`:
       - Use `@riverpod` annotation (Riverpod 3.x code-gen pattern).
       - Class `SearchNotifier extends _$SearchNotifier` (generated base).
       - `@override FutureOr<SearchResponse?> build() => null;` — null means no search yet.
       - Method `Future<void> search(String query) async`:
         - If query is empty after trim, return early.
         - `state = const AsyncLoading();`
         - `state = await AsyncValue.guard(() => ref.read(musicApiProvider).searchTracks(query));`
       - Run build_runner after creating this file: `flutter pub run build_runner build --delete-conflicting-outputs`

    2. Create `mobile/lib/features/search/widgets/source_badge.dart`:
       - A `SourceBadge` StatelessWidget taking `String source` parameter.
       - Renders a `Container` (styled as chip) with:
         - Height: 24dp, horizontal padding: 8dp, border radius: 12dp
         - Background color from the per-source map (per UI-SPEC):
           netease: Color(0xFFCC0000), qq: Color(0xFF1296DB), spotify: Color(0xFF1AA34A),
           youtube: Color(0xFFCC0000), ytmusic: Color(0xFFCC0000), tidal: Color(0xFF000000),
           qobuz: Color(0xFF1C3A6B), deezer: Color(0xFFA238FF), kugou: Color(0xFF2979FF),
           kuwo: Color(0xFFFF6E00), migu: Color(0xFFE31837), joox: Color(0xFF00C24B),
           default: Color(0xFF424242)
         - For tidal (black background): add a 1dp white border via `Border.all(color: Colors.white, width: 1)`
         - Text: source name, style 12sp/w400, color white
         - Tooltip: source name for accessibility
  </action>
  <verify>
    <automated>cd /Users/liou/py_project/musicdl/mobile && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mobile/lib/features/search/search_notifier.dart contains "AsyncNotifier" and "musicApiProvider" and "AsyncValue.guard"
    - mobile/lib/features/search/search_notifier.g.dart exists (generated)
    - mobile/lib/features/search/widgets/source_badge.dart contains "sourceBadgeColors" or the color map, and "0xFFCC0000" and "0xFF1AA34A" and "0xFFA238FF"
    - `flutter analyze --no-fatal-infos` exits 0
  </acceptance_criteria>
  <done>SearchNotifier manages async search state (null/loading/data/error). SourceBadge renders per-source colored chip with all 12+ source colors defined.</done>
</task>

<task type="auto">
  <name>Task 2: Track list tile, shimmer skeleton, empty/error states, and search screen assembly</name>
  <read_first>
    - .planning/phases/02-flutter-shell-search/02-UI-SPEC.md (all component specs: result card, shimmer, empty state, error state, search bar, typography, spacing, accessibility)
    - .planning/phases/02-flutter-shell-search/02-RESEARCH.md (Pitfall 6: shimmer wrapping)
    - mobile/lib/features/search/search_notifier.dart (from Task 1)
    - mobile/lib/features/search/widgets/source_badge.dart (from Task 1)
    - mobile/lib/core/models/track_dto.dart (TrackDto fields)
  </read_first>
  <files>
    mobile/lib/features/search/widgets/track_list_tile.dart,
    mobile/lib/features/search/widgets/shimmer_list.dart,
    mobile/lib/features/search/widgets/empty_state.dart,
    mobile/lib/features/search/widgets/error_state.dart,
    mobile/lib/features/search/search_screen.dart
  </files>
  <action>
    1. Create `mobile/lib/features/search/widgets/track_list_tile.dart`:
       - `TrackListTile` StatelessWidget taking `TrackDto track` and `VoidCallback onTap`.
       - Renders a `Card` (elevation 0, color: Color(0xFF1E1E1E)) containing a `ListTile`:
         - `leading`: `ClipRRect(borderRadius: 4dp)` wrapping `CachedNetworkImage`:
           - width/height: 48x48dp (per UI-SPEC locked dimension)
           - `imageUrl: track.coverUrl ?? ''`
           - `placeholder`: Container with Color(0xFF424242) and centered `Icons.music_note`
           - `errorWidget`: Container with `badgeColorFor(track.source ?? '')` background and centered `Icons.music_note` (platform-colored placeholder fallback per CONTEXT.md)
           - `semanticLabel`: "${track.songName ?? 'Unknown'} album art" (per accessibility contract)
         - `title`: Text(track.songName ?? 'Unknown', style: 16sp/w600, maxLines: 1, overflow: ellipsis)
         - `subtitle`: Column(crossAxisAlignment: start) with:
           - Text(track.singers ?? 'Unknown artist', style: 14sp/w400, maxLines: 1, overflow: ellipsis)
           - Row containing: Text(track.duration ?? '', style: 12sp/w400), SizedBox(width: 8), SourceBadge(source: track.source ?? '')
         - `onTap`: calls onTap callback
         - Minimum 48dp tap target height enforced by ListTile default

    2. Create `mobile/lib/features/search/widgets/shimmer_list.dart`:
       - `ShimmerList` StatelessWidget.
       - Per UI-SPEC and RESEARCH Pitfall 6: wrap the ENTIRE list in a SINGLE `Shimmer.fromColors` (not one per card):
         - `baseColor: Color(0xFF1E1E1E)`, `highlightColor: Color(0xFF2A2A2A)`
       - Child: `Column` with `List.generate(5, (_) => _SkeletonTile())`
       - `_SkeletonTile`: Padding(16dp horizontal, 8dp vertical) with Row:
         - Container 48x48dp with borderRadius 4dp, color white (shimmer will overlay)
         - SizedBox(width: 16)
         - Column: Container height 16, width 200 (title), SizedBox(height: 8), Container height 12, width 140 (subtitle)
       - Wrap in `ExcludeSemantics` (per accessibility contract — shimmer has no semantic meaning)

    3. Create `mobile/lib/features/search/widgets/empty_state.dart`:
       - `EmptyState` StatelessWidget with optional `bool hasSearched` parameter (default false).
       - When `hasSearched == false` (first launch):
         - Icons.music_note at 64dp, color Color(0xFF424242)
         - Heading: "Search for music" (20sp/w600)
         - Body: "Type a song, artist, or album above to find music from 21+ sources." (14sp/w400, color Color(0xFF9E9E9E))
       - When `hasSearched == true` (no results):
         - Icons.search_off at 64dp, color Color(0xFF424242)
         - Heading: "No music found for that search" (20sp/w600)
         - Body: "Try a different search term or check your connection." (14sp/w400, color Color(0xFF9E9E9E))
       - Centered in parent, 16dp horizontal padding.

    4. Create `mobile/lib/features/search/widgets/error_state.dart`:
       - `ErrorState` StatelessWidget with `VoidCallback onRetry`.
       - Centered content:
         - Icons.wifi_off at 64dp, color Color(0xFF424242)
         - Heading: "Couldn't reach the music server" (20sp/w600)
         - Body: "Check your connection and try again." (14sp/w400, color Color(0xFF9E9E9E))
         - ElevatedButton: "Retry Search", accent color Color(0xFF1DB954), 16sp/w600 text
         - Button has `Semantics(label: "Retry search")` wrapper

    5. Rewrite `mobile/lib/features/search/search_screen.dart`:
       - `SearchScreen` is a `ConsumerStatefulWidget`.
       - State manages a `TextEditingController` for the search field.
       - Tracks `_hasSearched` bool (starts false, set true after first submit).
       - AppBar: `title: Text('Search')` with background Color(0xFF121212).
       - Body column:
         - Search bar: `Padding(16dp)` wrapping `TextField` with:
           - `decoration: InputDecoration(hintText: 'Search songs, artists, albums...', prefixIcon: Icons.search in Color(0xFF9E9E9E), suffixIcon: clear button visible when text non-empty with semantic label "Clear search", filled: true, fillColor: Color(0xFF1E1E1E))`
           - `onSubmitted`: calls `ref.read(searchNotifierProvider.notifier).search(text)` and sets `_hasSearched = true`
           - `textInputAction: TextInputAction.search` (shows search button on keyboard)
           - Autofocus on first build (per UI-SPEC: search bar is primary focal point)
         - Expanded child: `ref.watch(searchNotifierProvider).when(...)`:
           - `data: (response)`:
             - If response == null: `EmptyState(hasSearched: false)` (first launch)
             - Else if response.tracks.isEmpty: `EmptyState(hasSearched: true)` (no results)
             - Else: `ListView.builder` rendering `TrackListTile` for each track, with `onTap` callback (wire to empty function for now — Plan 03 adds playback)
           - `loading: () => ShimmerList()`
           - `error: (e, _) => ErrorState(onRetry: () => ref.read(searchNotifierProvider.notifier).search(controller.text))`

    6. Run `flutter pub run build_runner build --delete-conflicting-outputs` if any @riverpod annotations were added.

    7. Verify: `flutter analyze --no-fatal-infos` exits 0.
  </action>
  <verify>
    <automated>cd /Users/liou/py_project/musicdl/mobile && flutter analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mobile/lib/features/search/widgets/track_list_tile.dart contains "CachedNetworkImage" and "48" (48dp art) and "SourceBadge" and "onTap"
    - mobile/lib/features/search/widgets/shimmer_list.dart contains "Shimmer.fromColors" and "0xFF1E1E1E" and "0xFF2A2A2A" and "ExcludeSemantics"
    - mobile/lib/features/search/widgets/empty_state.dart contains "Search for music" and "No music found for that search"
    - mobile/lib/features/search/widgets/error_state.dart contains "Couldn't reach the music server" and "Retry Search" and "0xFF1DB954"
    - mobile/lib/features/search/search_screen.dart contains "searchNotifierProvider" and "ShimmerList" and "EmptyState" and "ErrorState" and "TrackListTile" and "onSubmitted"
    - `flutter analyze --no-fatal-infos` exits 0
  </acceptance_criteria>
  <done>Search screen displays search bar with submit-on-enter, shimmer loading during search, result cards with album art + title + artist + duration + source badge, first-launch empty state, no-results empty state, and error state with retry button. All per UI-SPEC typography, colors, and spacing.</done>
</task>

</tasks>

<verification>
- `flutter analyze --no-fatal-infos` passes
- search_notifier.g.dart exists
- All 5 widget files exist under features/search/widgets/
- search_screen.dart imports and uses all widget components
- Source badge has 12+ source color mappings
- Shimmer uses single wrapper (not per-card) with ExcludeSemantics
- All UI-SPEC copywriting strings present in empty_state and error_state
</verification>

<success_criteria>
- User can type a keyword and press Enter to trigger search via API
- Shimmer skeleton shows while loading
- Results display as list tiles with album art, title, artist, duration, source badge
- First-launch shows friendly empty state
- No-results shows appropriate message
- Error shows retry button
- All strings match UI-SPEC copywriting contract exactly
</success_criteria>

<output>
After completion, create `.planning/phases/02-flutter-shell-search/02-02-SUMMARY.md`
</output>
